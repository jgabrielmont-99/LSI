---
layout: TCclass
title: 2.5. Esferas Rígidas
subtitle: Modelo de caroço duro (Hard-Sphere)
---

## Introdução ao Modelo
<p class="jt">
O modelo de <b>esferas rígidas</b> considera que as partículas ocupam um lugar no espaço...
(Pode colocar o texto teórico da aula aqui normalmente)
</p>

<style>
  .app-container {
    font-family: 'Segoe UI', sans-serif;
    /* COLE AQUI TODO O SEU CSS DO App.vue */
  }
</style>

{% raw %}
<div id="app" class="app-container">

  </div>
{% endraw %}

<script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
<script>
  const { createApp, ref, reactive, computed, onBeforeUnmount, nextTick } = Vue;

  createApp({
    setup() {
      // COLE AQUI TODA A SUA LÓGICA (variáveis, funções, classes)
      const params = reactive({ edge: 70, steps: 20000 /* ... */ });
      const isCalculating = ref(false);
      
      function runSimulation() {
         // ...
      }

      // NO FINAL DO SETUP, TEM DE RETORNAR TUDO O QUE O HTML VAI USAR
      return {
        params,
        isCalculating,
        runSimulation,
        // (liste todas as variáveis e funções usadas no HTML aqui)
      }
    }
  }).mount('#app');
</script>
